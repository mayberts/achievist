# ─────────────────────────────────────────────────────────────────────────────
# Gaming accounts (Steam, Xbox, RetroAchievements, Wargaming, Guild Wars 2,
# Ubisoft) are NO LONGER configured here. Connect them in the web UI under
# the "Accounts" tab — credentials are stored in the database.
#
# The variables below are app-level settings only.
# ─────────────────────────────────────────────────────────────────────────────

# --- Sync behaviour ----------------------------------------------------------
SYNC_INTERVAL_HOURS=12
REQUEST_DELAY_SECONDS=0.4

# --- Xbox --------------------------------------------------------------------
# "Sign in with Xbox" needs a free, one-time app registration (Microsoft only
# allows the device-code flow for your own client):
#   portal.azure.com → App registrations → New registration
#     (Personal Microsoft accounts, no redirect URI)
#   → Authentication → Allow public client flows → Yes
# Then set XBOX_CLIENT_ID to the Application (client) ID.
# Adding accounts by gamertag works once you've signed in.
XBOX_CLIENT_ID=

# --- IGDB cover art (optional) -----------------------------------------------
# Create a Twitch app: https://dev.twitch.tv/console/apps
# Fetches portrait cover art for non-Steam games as a fallback
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=

# --- SteamGridDB cover art (optional) ----------------------------------------
# Get your API key: https://www.steamgriddb.com/profile/preferences/api
# Fetches landscape grid art (460x215 / 920x430) for all platforms.
SGDB_API_KEY=

# --- Exophase (optional, Xbox 360 locked achievements) -----------------------
# Credentials extracted from browser session on exophase.com
EXOPHASE_PLAYER_ID=
EXOPHASE_REMEMBERME=
EXOPHASE_XF_USER=
EXOPHASE_ACCESS_TOKEN=
